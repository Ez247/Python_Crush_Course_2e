"""_More on range() function_: range(start, stop, skip)
        1. We tell the range where to start, where to stop, and to skip if necessary.
"""

#Printing even numbers in a range
even_numbers = list(range(2, 11, 2)) #Print even numbers between 1 and 10.
print(even_numbers)